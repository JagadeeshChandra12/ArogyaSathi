rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /hospitalFiles/{staffId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == staffId;
    }
  }
}
